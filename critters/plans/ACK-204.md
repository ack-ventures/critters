# ACK-204: Remove "Tips from usage" section from README

## Summary

Remove the "## Tips from usage" section from `README.md`. This section contains informal usage tips (lines 237–252) that are not present in `CLAUDE.md` and should be removed from the README.

## Files to modify

### `README.md`

**Change:** Delete lines 237–252 (the `## Tips from usage` heading and all 7 bullet points beneath it, plus the trailing blank line).

The section to remove:

```markdown
## Tips from usage

- **Create a `/critter` slash command in Claude Code** ...
- **Claude Max 20x is ideal for running lots of critters.** ...
- **Map projects to repos in the config** ...
- **Put a sentence or two of direction in the ticket description.** ...
- **Set up Slack notifications** ...
- **Plans are committed to `critters/plans/`** ...
- **Use a `/batch-review` slash command with Claude Code agent teams** ...
```

**What remains after the deletion:** The previous section (Multi-provider support) ends at line 235 with `See [CLAUDE.md](CLAUDE.md) for full multi-provider docs...`. After the deletion, the file continues with the `---` separator and the closing line (`See [CLAUDE.md](CLAUDE.md) for detailed developer docs...`).

No other files need changes — the task description confirms this section is not in `CLAUDE.md`.

## Dependencies / setup

None. This is a pure content removal from a single markdown file.

## Testing approach

1. Verify the "Tips from usage" heading and all its bullet points are gone from `README.md`
2. Verify the surrounding content (end of Multi-provider section + closing separator/line) is intact
3. Verify no other sections were accidentally modified
4. Run `git diff` to confirm only the expected lines were removed
