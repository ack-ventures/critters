# ACK-258: Update release command to only review README.md, not CLAUDE.md

## Summary
Remove the mention of `CLAUDE.md` from step 6 of `.claude/commands/release.md`, so the release process only reviews and updates `README.md`.

## Files to modify
- `.claude/commands/release.md` (line 10): Change step 6 from:
  > Review commits since last tag and check that README.md and CLAUDE.md are up to date with any new features, config fields, CLI changes, or behavioral changes introduced since the previous release. If anything is missing, add the relevant documentation now.

  To:
  > Review commits since last tag and check that README.md is up to date with any new features, config fields, CLI changes, or behavioral changes introduced since the previous release. If anything is missing, add the relevant documentation now.

## Changes
- Remove " and CLAUDE.md" from step 6, keeping everything else identical.
- No other steps or files need changes.

## Dependencies / Setup
None.

## Testing approach
- Read the file after editing to confirm only the intended change was made.
- Verify no other references to CLAUDE.md exist in the release command file.
