- [x] Step 1: Create `src/cli-kill.ts` — CLI command handler
- [x] Step 2: Modify `src/unified-spawner.ts` — Add `abortControllers` map, `killByIdentifiers()`, `killByType()`, `killAll()`, and `KillResult` interface
- [x] Step 3: Add "Killed via CLI" distinction in catch block (abort vs timeout)
- [x] Step 4: Modify `src/health.ts` — Add `POST /kill` endpoint with auth, validation, and `triggerKill` trigger
- [x] Step 5: Modify `src/index.ts` — Add `kill` subcommand routing, wire `triggerKill`, update help text
- [x] Step 6: Create `src/__tests__/cli-kill.test.ts` — 8 tests for kill endpoint
- [x] Step 7: Run typecheck (pass) and tests (767 pass, 2 pre-existing failures in validate.test.ts)
