- [x] Step 1: Update `src/health.ts` — pass `true` to `server.stop()` to force-close connections
- [x] Step 2: Update `src/index.ts` — add idempotency guard, hard fallback timer with `.unref()`
- [x] Step 3: Verify type-checking and tests pass (pre-existing failures only)
