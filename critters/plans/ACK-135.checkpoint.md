- [x] Step 1: Add `polling` flag and `triggerPoll()` to Watcher (src/watcher.ts)
- [x] Step 2: Add `polling` flag and `triggerPoll()` to ReviewWatcher (src/review-watcher.ts)
- [x] Step 3: Add `/poll` and `/review-poll` routes to health.ts
- [x] Step 4: Create cli-kickoff.ts
- [x] Step 5: Wire up kickoff subcommand and triggers in index.ts
- [x] Step 6: Add tests for new health routes (src/__tests__/health.test.ts)
- [x] Step 7: Type-check and run tests (all pass, 1 pre-existing failure in config.test.ts)
