- [x] Step 1: Update `src/index.ts` — always call `initFileLogging()` (removed `noTmux && !jsonLogs` guard)
- [x] Step 2: Update `src/logger.ts` `writeLog` — write to file AND console (removed `else` before console branches)
- [x] Step 3: Update `src/logger.ts` `writeJsonLog` — write to file AND stdout/stderr (removed `else` before stdout/stderr branches)
- [x] Step 4: Type-check passes, logger tests pass
- [x] Step 5: Commit, push, and create PR
