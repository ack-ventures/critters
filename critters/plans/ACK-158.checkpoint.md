- [x] Step 1: Modify `getRecentMetrics` in `src/metrics.ts` to use `.flatMap()` with per-line error handling
- [x] Step 2: Add `appendFileSync` import and corrupted-line test in `src/__tests__/metrics.test.ts`
- [x] Step 3: Run typecheck (`bun x tsc --noEmit`) — passes
- [x] Step 4: Run tests (`bun test`) — all pass (1 pre-existing failure in config.test.ts unrelated to this change)
