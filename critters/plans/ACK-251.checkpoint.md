- [x] Step 1: Create `src/pr-status.ts` — PR status fetching via `gh` CLI with caching
- [x] Step 2: Modify `src/dashboard.ts` — add `prStatuses` param, `renderPrStatusIcons` helper, update PR cells, add CSS
- [x] Step 3: Modify `src/health.ts` — collect PR URLs, fetch statuses, pass to render functions
- [x] Step 4: Type-check passes
