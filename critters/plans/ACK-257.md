# Plan: ACK-257 — Drastically reduce CLAUDE.md to essential dev guidance only

## Summary

The current `CLAUDE.md` is ~860 lines and is essentially a copy of the README plus additional reference docs. The README already covers: setup instructions, CLI commands/flags, configuration reference, dashboard endpoints, webhooks, hooks, custom critter types with examples, use cases, multi-provider support, Docker, and creating tickets.

The task is to strip `CLAUDE.md` down to ~50-80 lines containing only what a developer (or critter agent) needs to orient themselves in the codebase — information that isn't already in `README.md`.

## Files to modify

- **`CLAUDE.md`** — rewrite entirely. No other files will be changed (task explicitly says "Do NOT modify README.md").

### README cross-references (known consequence)

The README references CLAUDE.md in 4 places:
1. Line 248: prompt template variable reference (`{{identifier}}`, etc.)
2. Line 266: `CLAUDE.md#use-cases` for config examples
3. Line 309: multi-provider docs and Jira differences
4. Line 318: "detailed developer docs, architecture diagrams, and contributor conventions"

Since the task forbids modifying README.md, the slimmed-down CLAUDE.md will still satisfy reference #4 (it retains architecture + conventions). References #1-3 point to content that belongs in `README.md` or `docs/configuration.md` and should be redirected in a follow-up PR. The `docs/configuration.md` file already exists and is linked from README line 314 — it's the right home for detailed config reference material. Updating these README links is explicitly out of scope for this task.

## What to keep (with rationale)

1. **One-line project description** — orients the reader immediately
2. **Stack info** — Bun, TypeScript strict, key deps (quick reference, not in README in this compact form)
3. **Commands** — `bun install`, `bun start` / `bun run src/index.ts`, `bun run build`, `bun run lint`, `bun run typecheck`, `bun test` (dev workflow essentials; both `bun start` and `bun run src/index.ts` noted since README uses both)
4. **Conventions** — named exports only, logger usage, config loading, jq filter note (not in README)
5. **Architecture overview** — the ASCII flow diagram (abbreviated) + one-liner about how phases run. Just enough to orient, not the full deep dive
6. **Key files table** — abbreviated to ~15-18 most important files:
   - `src/index.ts` — entry point
   - `src/unified-watcher.ts` — poll loop
   - `src/unified-spawner.ts` — task lifecycle, phase pipeline
   - `src/critter-type.ts` — type definitions, synthesizeDefaultTypes()
   - `src/tracker/types.ts` — IssueTracker interface
   - `src/tracker/linear.ts` — Linear tracker
   - `src/tracker/jira.ts` — Jira tracker
   - `src/runner/types.ts` — PhaseRunner interface
   - `src/runner/planning.ts` — planning phase
   - `src/runner/execution.ts` — execution phase
   - `src/runner/review.ts` — review phase
   - `src/runner/generic.ts` — custom type phase runner
   - `src/cli/claude.ts` — Claude Code CLI adapter
   - `src/cli/spawn.ts` — tmux/subprocess spawning
   - `src/prompt-template.ts` — prompt resolution, variable substitution
   - `src/config.ts` — config loading
   - `src/git.ts` — clone, branch, commit, cleanup
   - `critters/plans/<ID>.md` — planning phase output (critters write plans here)

## What to remove (all covered in README or reference docs)

- Getting Started / Prerequisites / Setup (README §Development quick start)
- Creating tickets / Creating review tickets (README §Creating tickets)
- Custom Critter Types full section with config reference table (README §Custom critter types + docs/configuration.md)
- Use Cases with all YAML examples (README §Use cases)
- Multi-Provider Support full section (README §Multi-provider)
- Config reference table (README §Configuration)
- Allowed tools section (README §Configuration)
- Environment variables (README §Development quick start + §Configuration)
- Build & Binary (README mentions build; details are CI-specific)
- CLI Commands and Daemon Flags (README §Commands and §Flags)
- JSON Log Format (operational detail)
- Release / Versioning (CI/release process)
- Per-repo Configuration (README mentions it)
- Hooks full section (README §Hooks)
- Slack Notifications (README mentions it)
- Dashboard & Health Server (README §Web Dashboard)
- Webhooks (README §Webhooks)
- Remote Access / ngrok (README §Configuration mentions tunnel)
- Skills section (config detail, not dev guidance)
- Model guidance (README §Custom critter types covers this)
- Testing custom types (operational)

## Proposed new CLAUDE.md structure (~75 lines)

```markdown
# Critters

TypeScript daemon that watches issue trackers (Linear/Jira) for labeled issues, spawns Claude Code CLI instances to work on them, and produces draft PRs.

## Stack
- Runtime: Bun
- Language: TypeScript (strict)
- Package manager: bun
- Key deps: @linear/sdk, yaml

## Commands
- `bun install` — install dependencies
- `bun start` — run the daemon (or `bun run src/index.ts`)
- `bun run build` — compile binary
- `bun run lint` — lint
- `bun run typecheck` — type check
- `bun test` — run tests

## Conventions
- All source in `src/`
- No default exports — use named exports
- Use `console` via `src/logger.ts` wrapper (timestamped)
- Config loaded from `critters.config.yaml` + `.env`
- jq filter for stream output lives in `src/stream-filter.jq` (not a JS template — edit directly)

## Architecture

​```
Watcher (src/unified-watcher.ts) — polls trackers per critter type
    ▼
Spawner (src/unified-spawner.ts) — per-type queues, lifecycle
    │  clone → branch → run phases → handle outcomes
    ▼
Phase Runners (src/runner/)
    ├─ PlanningPhaseRunner  (builtin:planning)
    ├─ ExecutionPhaseRunner (builtin:execution)
    ├─ ReviewPhaseRunner    (builtin:review)
    └─ GenericPhaseRunner   (custom prompts → .critter-report.md)
​```

Each phase runs `claude -p` with `--output-format stream-json` in a tmux pane, piped through jq for display.

## Key files

| File | Purpose |
|---|---|
| `src/index.ts` | Entry point, wiring, signal handlers |
| `src/unified-watcher.ts` | Single poll loop for all critter types |
| `src/unified-spawner.ts` | Per-type queues, lifecycle, phase pipeline |
| `src/critter-type.ts` | CritterTypeConfig, synthesizeDefaultTypes() |
| `src/tracker/types.ts` | IssueTracker interface, TrackerTask |
| `src/tracker/linear.ts` | LinearTracker (wraps Linear SDK) |
| `src/tracker/jira.ts` | JiraTracker (Jira Cloud REST API) |
| `src/runner/types.ts` | PhaseRunner interface, PhaseContext |
| `src/runner/planning.ts` | Planning phase (plan + reviewer loop) |
| `src/runner/execution.ts` | Execution phase (implement, commit, push, PR) |
| `src/runner/review.ts` | Review phase (diff review, approve/merge) |
| `src/runner/generic.ts` | Generic phase (custom types, .critter-report.md) |
| `src/cli/claude.ts` | ClaudeCodeAdapter (CLI integration) |
| `src/cli/spawn.ts` | tmux/subprocess spawning |
| `src/prompt-template.ts` | resolvePrompt(), variable substitution |
| `src/config.ts` | Load YAML + env, parse critterTypes |
| `src/git.ts` | Clone, branch, commit, cleanup |
| `critters/plans/<ID>.md` | Planning phase output (committed to target repo) |

See `README.md` for setup, configuration, CLI commands, custom types, and usage docs.
```

## Dependencies / setup needed

None. This is a single-file edit.

## Testing approach

1. Verify the new CLAUDE.md is within the 50-80 line target
2. Verify no content was removed that ISN'T covered by README.md or docs/configuration.md
3. Verify all "keep" items from the task guidelines are present: stack/runtime, conventions, build/run/test commands, brief architecture, abbreviated key files
4. Verify README.md is not modified
5. Count lines to confirm target range

## Previous Review Items

> [MUST_FIX] The README.md references CLAUDE.md in 4 places for detailed content that the plan proposes to remove. If these sections are removed without updating README.md, the README will have broken or misleading cross-references.

Addressed: Added a dedicated "README cross-references" subsection under "Files to modify" that explicitly acknowledges all 4 references, explains which are still satisfied by the slimmed-down CLAUDE.md (#4 — architecture + conventions), and notes that redirecting #1-3 to `docs/configuration.md` is out of scope per the task requirement "Do NOT modify README.md". This is a known consequence to be handled in a follow-up PR.

> [SHOULD_FIX] The plan's proposed "Key files" section was only placeholder text and should explicitly list which cli/ files make the cut.

Addressed: The "What to keep" section now explicitly lists all 18 files including `src/cli/claude.ts` and `src/cli/spawn.ts`. The proposed CLAUDE.md structure includes the full rendered table with all files specified.

> [SHOULD_FIX] The plan should note that both `bun start` and `bun run src/index.ts` work.

Addressed: The Commands section now shows `bun start` — run the daemon (or `bun run src/index.ts`)`, noting both forms.

> [CONSIDER] Preserve a note about `critters/plans/<ID>.md` convention.

Addressed: Added as the last row in the key files table with description "Planning phase output (committed to target repo)".

> [CONSIDER] Verify docs/configuration.md exists.

Noted: README line 314 links to `docs/configuration.md`. Its existence means detailed config reference material has a proper home outside CLAUDE.md. The specific content of that file doesn't affect this plan — we're only removing content from CLAUDE.md that's redundant with README, not moving content into docs/.
