# ACK-274: Document the review/fix-review-comments critter loop in README

## Summary

Add a short section to `README.md` that explains the automated create → review → fix-review-comments loop. The built-in `create` and `review` types handle PR creation and review respectively. When the review critter requests changes, the issue moves to "Human Review" status — by default requiring human intervention (or `critters retry --force`). Users can configure a custom `fix-review-comments` critter type that triggers on "Human Review", fixes the PR based on review feedback, and moves the issue back to "In Review", creating an automated review loop.

## Files to modify

### `README.md`

**Location:** Insert a new section after the existing "How it works" section (after line 113, before "## Configuration").

**Content:** A new `## Critter lifecycle` section that explains:

1. **The default flow:** `create` picks up "Todo" issues, claims them as "In Progress", plans and implements, opens a PR, and moves to "In Review". The `review` critter picks it up and either merges (→ "Done") or requests changes (→ "Human Review").
2. **The loop:** By adding a `fix-review-comments` custom critter type that triggers on "Human Review" status, you can automate the fix cycle. After fixing, it sets status back to "In Review", and the review critter picks it up again.
3. **A concise ASCII diagram** showing the status transitions including the loop.
4. **A short YAML snippet** showing the `fix-review-comments` type config using a custom prompt file (NOT `builtin:execution`, since `ExecutionPhaseRunner` references a plan file from a prior planning phase that won't exist). The snippet includes `claimStatus` to prevent duplicate dispatch.

**Estimated addition:** ~35-45 lines of markdown.

**Section content:**

```markdown
## Critter lifecycle

The built-in critter types form a pipeline:

```
Todo → In Progress → In Review → review ──┬──► Done (merged)
         (create)                          │
                        ┌──────────────────┘
                        ▼
                   Human Review
                        │
                        └──► fix-review-comments ──► In Review (loops back to review)
```

1. **create** picks up "Todo" issues, plans and implements the work, opens a draft PR, and moves the issue to "In Review".
2. **review** picks up "In Review" issues, reviews the PR diff, and either merges it (→ "Done") or requests changes (→ "Human Review").
3. **fix-review-comments** (optional, user-configured) picks up "Human Review" issues, reads the review feedback from the PR, addresses it, and moves the issue back to "In Review" — sending it through review again.

Without `fix-review-comments`, issues that need changes stop at "Human Review" for manual intervention (or `critters retry --force`).

To enable the loop, add a `fix-review-comments` type to your config:

```yaml
critterTypes:
  # ... create and review types ...

  fix-review-comments:
    trigger: { label: "Critter Review", status: "Human Review" }
    repo: { clone: true, branch: true }
    phases:
      - name: fix
        prompt: ~/.critters/prompts/fix-review.md
        model: opus
        maxTurns: 50
        tools: default
    outcomes:
      success: { status: "In Review" }
      failure: { status: "Critter Failed" }
    concurrency: 2
    timeoutMinutes: 20
    enrichment: extractPrUrl
    claimStatus: "In Progress"
```

The prompt file should instruct Claude to read the PR review comments (`gh pr view` and `gh pr reviews`), make the requested fixes, commit, and push. Use `builtin:execution` prompts as a starting point. `claimStatus` prevents duplicate dispatch if two poll cycles trigger before the fix completes.
```

## Files to create

None.

## Dependencies / setup

None — this is a documentation-only change.

## Testing approach

- Visual review of the README diff to confirm the new section is clear, accurate, and placed correctly.
- Verify the YAML snippet is syntactically valid and the config fields match the `CritterTypeConfig` interface in `src/critter-type.ts`.
- Confirm the status transitions described match the actual code:
  - `create` outcomes: `success → "In Review"`, `failure → "Critter Failed"` (from `critters.config.yaml:64-65`), with `claimStatus: "In Progress"` (from `src/critter-type.ts:80`)
  - `review` outcomes: `merged → "Done"`, `needsChanges → "Human Review"`, `failure → "Critter Failed"` (from `critters.config.yaml:79-81`)
  - `fix-review-comments` proposed outcomes: `success → "In Review"` (re-enters the review loop), `failure → "Critter Failed"`
- Verify the `fix-review-comments` trigger config is plausible: `label: "Critter Review"` matches the review type's label, `status: "Human Review"` matches the review type's `needsChanges` outcome status.
- Verify `enrichment: extractPrUrl` is included so the fix type can find the PR to work on.
- Verify that a custom prompt path is used instead of `builtin:execution`, since `ExecutionPhaseRunner.buildExecutionPrompt` (in `src/prompt.ts:225`) references `critters/plans/<ID>.md` which won't exist for this type.
- Verify `claimStatus: "In Progress"` is included to prevent duplicate dispatch (since `trigger.statusType` is not "unstarted", `parseCritterType` in `src/critter-type.ts:204` won't auto-assign one).

## Previous Review Items

> [SHOULD_FIX] The YAML snippet uses `builtin:execution` as the phase prompt, but `ExecutionPhaseRunner` builds a prompt via `buildExecutionPrompt` that references a plan file from a prior planning phase. A `fix-review-comments` type with only a single execution phase would have no plan file, which may cause Claude to behave unexpectedly (looking for a nonexistent plan). The snippet should either use a custom prompt file (e.g., `~/.critters/prompts/fix-review.md`) or at minimum, the documentation text should note that a custom prompt is recommended and explain why `builtin:execution` may not be ideal for this use case.

Addressed: Changed the YAML snippet to use `~/.critters/prompts/fix-review.md` instead of `builtin:execution`. Added explanatory text noting that the prompt should instruct Claude to read PR review comments, and suggesting users reference `builtin:execution` prompts as a starting point.

> [SHOULD_FIX] The proposed `fix-review-comments` config lacks a `claimStatus` field. Without `claimStatus`, duplicate dispatch is possible if two poll cycles trigger before the fix completes. The YAML snippet should include `claimStatus: "In Progress"` (or similar) and the documentation text should mention why this matters.

Addressed: Added `claimStatus: "In Progress"` to the YAML snippet and added a sentence explaining that it prevents duplicate dispatch.

> [CONSIDER] The lifecycle diagram omits the "In Progress" intermediate status that appears in the existing "How it works" section immediately above the proposed insertion point.

Addressed: Updated the diagram to include "In Progress" as an intermediate status in the create flow.

> [CONSIDER] The plan summary describes the default behavior as "a dead end requiring human intervention" — `critters retry --force` is also an option.

Addressed: Updated the summary and section text to mention `critters retry --force` as an alternative.
